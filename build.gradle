apply plugin: 'java'

// 加入外部依赖
repositories {
    mavenCentral()
}

//
sourceCompatibility = 1.8
version = '1.0'

jar {
    manifest {
//        attributes 'Implementation-Title': 'Gradle Quickstart', 'Implementation-Version': version, 'Main-Class':'com.gxl.example.jvm.HelloWorld'
    }
}

uploadArchives {
    repositories {
       flatDir {
           dirs 'repos'
       }
    }
}


